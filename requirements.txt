# 视频翻译系统依赖包
# Python 3.8+

# 核心依赖
asyncio-timeout>=4.0.0
websockets>=11.0.0
python-dotenv>=1.0.0

# 视频/音频处理
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# 音频处理
wave
librosa>=0.10.0
soundfile>=0.12.0

# 网络请求
requests>=2.31.0
aiohttp>=3.8.0

# 数据处理
dataclasses; python_version<"3.7"

# 日志和工具
colorlog>=6.7.0
tqdm>=4.65.0

# 开发和测试依赖
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.7.0
flake8>=6.0.0
mypy>=1.5.0

# 文档生成
sphinx>=7.1.0
sphinx-rtd-theme>=1.3.0

# 可选依赖（性能优化）
uvloop>=0.17.0; sys_platform != "win32"
orjson>=3.9.0

# FFmpeg Python绑定（可选，如果系统没有ffmpeg命令）
# ffmpeg-python>=0.2.0

# 注意：
# 1. 需要系统安装 ffmpeg 和 ffprobe 命令行工具
# 2. 需要配置阿里云百炼平台API密钥
# 3. Python版本建议 3.8 或以上